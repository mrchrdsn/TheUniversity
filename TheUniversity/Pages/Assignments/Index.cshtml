@page
@model TheUniversity.Pages.Assignments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Assignment[0].AssignmentDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Assignment[0].AssignmentGrade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Assignment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AssignmentDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssignmentGrade)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AssignmentID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AssignmentID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AssignmentID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
